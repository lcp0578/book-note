## 第4章 文本分类

- 数据被分为**训练集**、**测试集**和**验证集**。
- 使用预训练表示模型进行分类，通常有两种方式：要么使用特定任务模型，要么使用嵌入模型。
- 评估模型性能的四个常用的指标
  - 精确率(precision)
    - 衡量模型识别出的结果中有多少是相关的，用来评估相关结果的准确性。
  - 召回率(recall)
    - 指的是所有相关类别中有多少被成功识别出来，用来评估模型找到所有相关结果的能力。
  - 准确率(accuracy)
    - 指的是模型在所有预测中作出正确预测的比例，用来评估模型的整体准确性。
  - F1 分数(F1 score)
    - 平衡了精确率和召回率，用于衡量模型的整体性能。
- 使用特定任务模型
- 利用嵌入向量的分类任务
  - 监督分类
- 使用生成模型进行文本分类
  - 特定任务模型从词元序列生成数值，而生成模型从词元序列生成词元序列。
  - 我们需要帮助它理解上下文，并引导它得出我们想要的答案。这种引导过程主要通过给模型输入指令或**提示词**来完成。迭代改进提示词以获得期望的输出的过程被称为**提示工程**(prompt engineering)。
- 使用T5(Text-to-Text Transfer Transformer, 文本到文本迁移Transformer)
